const names = ["Guadalupe", "Ollie", "Aki"]

function writeCards(names, messages){
    let newArr = []
    for (let i = 0; i < names.length; i++) {
    return newArr.map(`Thank you ${names[i]} for the wonderfl suprise gifts!`);
  }

}
